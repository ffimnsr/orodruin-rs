# yaml-language-server: $schema=https://raw.githubusercontent.com/ffimnsr/mk-rs/main/docs/schema.json

use_cargo: true

tasks:
  greet:
    commands:
      - echo "Hello, World!"
    description: Sample greet command
  install:
    commands:
      - cargo install --force --path crates/orodruin --all-features
